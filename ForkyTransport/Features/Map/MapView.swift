import SwiftUI

struct MapView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("Mapa")
                    .font(.largeTitle)
            }
            .navigationTitle("Mapa")
        }
    }
}
